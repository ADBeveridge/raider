#TODO this build needs to download adwaita and build - if this fails
# fallback to the system version of libadaita.

project('adwaita', 'c', version : '2.0.0')

lib_adwaita_dep = dependency('libadwaita-1')
